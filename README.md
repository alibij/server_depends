# server_depends
This repository provides a **one-command setup script** for initializing a modern development environment on **Ubuntu/Debian-based systems**.   It installs and configures essential tools such as **Docker**, **Docker Compose**, **NGINX**, **Git**, **Zsh**, **eza (ls replacement)**, and more —   with **optional SOCKS5/HTTP proxy support**
